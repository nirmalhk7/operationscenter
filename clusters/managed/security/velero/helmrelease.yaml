apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app velero
  namespace: mgd-security
spec:
  chart:
    spec:
      chart: velero
      version: 8.7.2
      sourceRef:
        kind: HelmRepository
        name: vmware-tanzu
        namespace: mgd-security
  interval: 1h
  releaseName: *app