apiVersion: v1
kind: Service
metadata:
  name: &app pterodactyl-lb
  namespace: mgd-gaming
  labels:
    milano.app: pterodactyl
spec:
  type: LoadBalancer
  ports:
    - name: web
      port: 80
      targetPort: 80
      nodePort: 31217
  selector:
    milano.app: pterodactyl
