apiVersion: v1
kind: Service
metadata:
  name: &app pterodactyl-redis
  namespace: mgd-gaming
  labels:
    milano.app: *app
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    milano.app: *app
