apiVersion: v1
kind: Secret
metadata:
  name: pterodactyl-wings-config
  namespace: mgd-gaming
type: Opaque
stringData:
  config.yml: |
    # placeholder — replace via kubeseal after creating the Node in the Panel UI.
    # See clusters/managed/gaming/pterodactyl-wings/README-bootstrap in the plan
    # file, or the project plan at ~/.claude/plans/recursive-brewing-snowflake.md
