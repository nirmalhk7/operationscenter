apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: &app pterodactyl-wings-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: milano-v2
  resources:
    requests:
      storage: 50Gi
