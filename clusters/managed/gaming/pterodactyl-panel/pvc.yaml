apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: &app pterodactyl-data
  namespace: mgd-gaming
  labels:
    milano.app: *app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: milano-v2
  resources:
    requests:
      storage: 5Gi
