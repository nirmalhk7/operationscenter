
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: &app mgd-headervalidation
spec:
  headers:
    customResponseHeaders:
      X-Server: "OCB-Milano"
    frameDeny: true
    browserXssFilter: true