apiVersion: v1
kind: Service
metadata:
  name: freshrss
  labels:
    app.kubernetes.io/name: freshrss
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 31217
  selector:
    app.kubernetes.io/name: freshrss
