server {
    listen 127.0.0.1:7777;
    location /prometheus {
        stub_status;
        allow 127.0.0.1;
        deny all;
        add_header Milano-Resource "OCB-prometheus";
    }
    location / {
        return 404;
    }
}
