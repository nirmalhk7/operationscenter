---
- name: Bootstrap Flux-managed cluster reconciliation
  hosts: vm_k8mgd
  become: true
  gather_facts: false
  tasks:
    - name: Run Flux bootstrap role
      ansible.builtin.import_role:
        name: ../roles/flux_bootstrap
